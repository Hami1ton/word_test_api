spring:
  jpa:
    hibernate:
      ddl-auto: none # データのリセットをふせぐため
  datasource:
    initialization–mode: always
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=MySQL;
    username: username
    password:
  h2:
    console:
      enabled: true

server:
  port: 5000